name: Build and Test

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Setup
        run: apt install cppcheck clang-format
      - name: Build
        run: make
      - name: Test
        run: make test
